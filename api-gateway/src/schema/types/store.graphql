type Store {
  id: ID
  name: String
  address: String
  tel: String
  phone: String
}

type Query {
  getStore(id: String!): Store
}

type Mutation {
  newStore(input: StoreInput): Store
}

input StoreInput {
  id: ID
  name: String
  address: String
  tel: String
  phone: String
}
